apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-xtls
  namespace: flux-system
spec:
  interval: 10m
  wait: true
  timeout: 5m
  path: ./src/apps/xtls
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: infra-envoy-config
  decryption:
    provider: sops
    secretRef:
      name: sops-age
