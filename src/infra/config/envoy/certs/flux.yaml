apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: flux-tls
  namespace: envoy-gateway
spec:
  secretName: flux-tls
  issuerRef:
    name: cloudflare-issuer
    kind: ClusterIssuer
  dnsNames:
    - cloud.lxft.dev
