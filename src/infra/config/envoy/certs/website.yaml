apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: website-tls
  namespace: envoy-gateway
spec:
  secretName: website-tls
  issuerRef:
    name: cloudflare-issuer
    kind: ClusterIssuer
  dnsNames:
    - lxft.dev
